package piscine

func IsAlpha(s1 string) bool {
	r := []rune(s1)
	for u := 0; u < len(u); u++ {
		if (r[a] < 'a' || r[a] > 'z') && (r[a] < 'A' || r[a] > 'Z') && (r[a] < '0' || r[a] > '9') {
			return false
		}
	} 
	return true
}
